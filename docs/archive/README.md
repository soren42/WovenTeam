# Archive

Superseded documentation kept for historical reference only. **These files do
not describe the current system** — see the wiki, `docs/user-manual/`, and
`docs/dev-guide/` for v1.0.x documentation.

- `phase0-cheat-sheet.html` — Phase 0 operator cheat sheet (workers exec'd
  literal shell commands rather than invoking models). Superseded by the
  v1.0.2 User Manual.
