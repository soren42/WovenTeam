#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
if ! make -C "$ROOT" -q build/wt-roomd; then
  echo "wt-roomd binary is missing or stale; building..." >&2
  make -C "$ROOT" build/wt-roomd >&2
fi

exec "$ROOT/build/wt-roomd" "$@"
